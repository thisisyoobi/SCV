#include <stdio.h>
#include <stdlib.h>
#define _CRT_SECURE_NO_WARNINGS

void U_68()
{
	char buf[512];
	FILE* fp;
	printf("[U-68] SNMP 서비스 Community String의 복잡성 설정 (중) : 수동 확인\n");
	fclose(fp);
	return;
}